
<main class="container">

    {{#each posts}}
        <div>
            <h2>{{this.title}}</h2>
            <p>{{this.body}}</p>
            <p>Posted by: {{this.username}}</p>
            <p>Date: {{this.date}}</p>
        </div>
    {{/each}}
</main>

