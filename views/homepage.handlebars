<main class="container">

  {{#each posts}}
    <div>
      <h2>{{this.title}}</h2>
      <p>{{this.body}}</p>
      <p>Posted by: {{this.username}}</p>
      <p><strong>Date:</strong> {{formatDate createdAt}}</p>
    </div>
  {{/each}}
</main>